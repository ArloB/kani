// Semantic validation: DSL parsing, field type checking, required endpoint presence.

use super::schema::YamlExtension;

pub fn validate(_ext: &YamlExtension) -> Result<(), String> {
    todo!("yaml semantic validation")
}
