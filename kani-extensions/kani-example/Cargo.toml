[package]
name = "kani-example"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kani-shared = { path = "../../kani-shared" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
