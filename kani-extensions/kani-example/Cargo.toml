[package]
name = "kani-example"
version = "0.1.0"
edition = "2024"

[package.metadata]
id = "example"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kani-shared = { path = "../../kani-shared" }
