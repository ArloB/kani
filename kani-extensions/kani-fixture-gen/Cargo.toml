[package]
name = "kani-fixture-gen"
version = "0.1.0"
license.workspace = true
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kani-shared = { path = "../../kani-shared", features = ["builder"] }

[package.metadata]
id = "fixture-gen"
