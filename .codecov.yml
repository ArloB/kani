coverage:
  status:
    project:
      default:
        # Small tolerance: a refactor that moves covered code around should not
        # fail the build, but a sustained decline should.
        threshold: 1%
    patch:
      default:
        threshold: 2%

comment:
  layout: "reach, diff, flags, files"
  require_changes: true
