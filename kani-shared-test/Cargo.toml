[package]
name = "kani-shared-test"
version = "0.1.0"
edition = "2024"
publish = false

[lints.clippy]
unwrap_used = { level = "deny", priority = 1 }

[dependencies]
kani-app = { path = "../kani-app", features = ["test-util"] }
sqlx.workspace = true
