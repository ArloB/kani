[package]
name = "kani-shared"
description = "Shared types and traits for Kani."
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
wit-bindgen = "0.53.1"
serde_json = "1.0"
base64 = "0.22.1"
